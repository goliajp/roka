[workspace]
resolver = "2"
members = [
    "crates/qr",
    "crates/totp",
    # crates/app will land here when lab10-2fa reorganizes in — that's the `roka` product binary.
]

[workspace.lints.rust]
warnings = "deny"

[workspace.lints.clippy]
all = { level = "deny" }
